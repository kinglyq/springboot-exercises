<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="priv.lyq.springboot.multipleds.mapper.UserMapper">

    <resultMap id="user" type="priv.lyq.springboot.multipleds.entity.User">
        <id property="id" column="uid"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <collection property="authorities" ofType="priv.lyq.springboot.multipleds.entity.Role">
            <id property="id" column="rid"/>
            <result property="name" column="name"/>
        </collection>
        <collection property="pages" ofType="priv.lyq.springboot.multipleds.entity.Page">
            <id property="id" column="pid"/>
            <result property="addr" column="addr"/>
            <collection property="buttons" ofType="priv.lyq.springboot.multipleds.entity.Button">
                <id property="id" column="bid"/>
                <result property="hyperlink" column="hyperlink"/>
            </collection>
        </collection>
    </resultMap>

    <select id="selectUserByName" resultMap="user">
        select u.id AS "uid",
               u.username,
               CONCAT("{bcrypt}", u.password) AS "password",
               r.id AS "rid",
               r.name,
               p.id AS "pid",
               p.addr,
               p.icon,
               b.id AS "bid",
               b.hyperlink
        from user u
                 left join user_role ur on
            u.id = ur.user_id
                 left join role r on
            r.id = ur.role_id
                 left join role_button rb on
            r.id = rb.role_id
                 left join button b on
            b.id = rb.button_id
                 left join page_button pb on
            pb.id = b.id
                 left join page p on
            p.id = pb.page_id
        where u.id = #{username}
    </select>

</mapper>